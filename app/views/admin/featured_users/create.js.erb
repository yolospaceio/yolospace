<% if @errors.present? %>
  <%= custom_error_flash(@errors, 'error') %>
<% else %>
  <% flash.keep[:notice] = 'Featured Users Successfully created.' %>
  window.location.replace("<%= admin_featured_users_path %>");
<% end %>